//This needs to match the json data exactly

export type Movie = {
  movieId: number
  category: string
  title: string
  year: number
  director: string
  rating: string
  edited: string
  lentTo: string
  notes: string
}
